import type {ReactNode} from 'react';
import Layout from '@theme/Layout';


export default function Home(): ReactNode {
  return (
    <Layout>
      <main>
        To do: build landing page
      </main>
    </Layout>
  );
}
